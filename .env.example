# Copy this file to .env and fill in your credentials.
# These values are used ONLY by the Vercel serverless function (api/tb.js)
# and are NEVER sent to the browser.

TB_HOST=https://thingsboard.cloud
TB_USER=your-email@example.com
TB_PASS=your-password
